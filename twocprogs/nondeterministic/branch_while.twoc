decr_on_zero = true;
alphabet = [ '0' ];

twoc (int X) {
    c = X;
    goto(lend);

    while (read != rend) {
        branch {
            move(1);
            if (read == rend) { accept; }
        } also {
            c--;
            if (c == 0) { accept; }
        }
    }
}