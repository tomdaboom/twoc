alphabet = [ '0' ]

twoc (string) {
    while (read != rend) {
        c++;
        move(1);
    }

    while (read != lend) { move(-1); }

    while (c != 0) {
        while (read != rend) { move(1); }
        while (read != lend) { move(-1); }
        c--;
    }

    accept;
}